---
# HTTPRoute for ArgoCD Server
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd
  namespace: rex5-cc-mz-prod-argocd
  labels:
    app.kubernetes.io/name: argocd
spec:
  parentRefs:
    - name: platform-traefik-gateway
      namespace: rex5-cc-mz-prod-traefik
      sectionName: websecure
  hostnames:
    - argocd.rex5.ca
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: argocd-server
          port: 80

---
# HTTP to HTTPS redirect
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-http-redirect
  namespace: rex5-cc-mz-prod-argocd
  labels:
    app.kubernetes.io/name: argocd
spec:
  parentRefs:
    - name: platform-traefik-gateway
      namespace: rex5-cc-mz-prod-traefik
      sectionName: web
  hostnames:
    - argocd.rex5.ca
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
