apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - httproute.yaml
  - externalsecret.yaml

# Note: ArgoCD is installed manually via Helm during bootstrap
# This kustomization manages additional resources like HTTPRoute
