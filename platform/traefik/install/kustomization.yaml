apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Note: Traefik is installed manually via Helm during bootstrap
# This kustomization is for Helm-related metadata/resources if needed

resources: []
