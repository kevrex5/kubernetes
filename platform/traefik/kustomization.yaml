apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - gateway.yaml
  - externalsecret.yaml
  - externalsecret-wildcard.yaml
  - networkpolicies.yaml

# Values are handled via Helm in ArgoCD ApplicationSet
# This kustomization manages additional resources
